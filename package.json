{
  "name": "telegram-verify-bot",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev --remote"
  },
  "devDependencies": {
    "wrangler": "^3.91.0"
  }
}
